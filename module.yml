api: "1.0"
name: "clock"
type: node
script: ./server/index.js
config:
  - fields:
    - name: clockFormat
      display: Clock format
      type: values
      default: clock
      values: [clock, seconds]